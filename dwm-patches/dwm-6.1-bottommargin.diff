diff --git a/config.def.h b/config.def.h
index 7054c06..5e7ab71 100644
--- a/config.def.h
+++ b/config.def.h
@@ -15,6 +15,7 @@ static const unsigned int borderpx  = 1;        /* border pixel of windows */
 static const unsigned int snap      = 32;       /* snap pixel */
 static const int showbar            = 1;        /* 0 means no bar */
 static const int topbar             = 1;        /* 0 means bottom bar */
+static const unsigned bottommargin = 16;    /*Bottom margin in pixel*/
 
 /* tagging */
 static const char *tags[] = { "1", "2", "3", "4", "5", "6", "7", "8", "9" };
diff --git a/dwm.c b/dwm.c
index 314adf4..199a6b6 100644
--- a/dwm.c
+++ b/dwm.c
@@ -1743,6 +1743,9 @@ updatebarpos(Monitor *m) {
 	m->wh = m->mh;
 	if(m->showbar) {
 		m->wh -= bh;
+		m->wh -= bottommargin; 
+		if(!m->topbar) 
+		  m->wy += bottommargin; 
 		m->by = m->topbar ? m->wy : m->wy + m->wh;
 		m->wy = m->topbar ? m->wy + bh : m->wy;
 	}
